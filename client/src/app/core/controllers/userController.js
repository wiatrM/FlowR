'use strict';

angular.module('flr.core')
    .controller('UserController',
        function ($auth, $log, $http, $rootScope) {

        var vm = this;

        // vm.logout = function() {
        //     $auth.logout().then(function() {
        //         $rootScope.authenticated = false;
        //         $rootScope.currentUser = null;
        //     });
        // };

});
